name: Build ImmortalWrt

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex g++ gawk gcc-multilib \
          gettext git libncurses5-dev libssl-dev python3 rsync unzip zlib1g-dev \
          file wget

      - name: Download ImageBuilder
        run: |
          wget https://downloads.immortalwrt.org/releases/23.05.3/targets/mvebu/cortexa9/immortalwrt-imagebuilder-23.05.3-mvebu-cortexa9.Linux-x86_64.tar.zst
          tar --zstd -xf immortalwrt-imagebuilder-*.tar.zst
          mv immortalwrt-imagebuilder-* builder

      - name: Build firmware
        run: |
          cd builder && mkdir -p files/etc/init.d && printf '#!/bin/sh /etc/rc.common\nSTART=10\nstart(){ uci set system.@system[0].hostname="NGU-NOOH-TIKTOK"; uci commit system; }\n' > files/etc/init.d/force-hostname && chmod +x files/etc/init.d/force-hostname && make image PROFILE=linksys_wrt1900acs PACKAGES="luci luci-base luci-mod-admin-full luci-theme-argon luci-app-argon-config sqm-scripts collectd-mod-sqm kmod-sched-cake kmod-sched-ctinfo kmod-sched-core kmod-sched-connmark kmod-ifb luci-app-irqbalance luci-app-cpulimit luci-app-cpufreq node-argon2 -luci-mod-system"

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: builder/bin/targets/
